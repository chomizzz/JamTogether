<div class="flex justify-around w-full bg-gray-50 w-full justify-around rounded-b-lg drop-shadow-lg">

  <div class="flex">Nom de la salle : <%= @room.name %></div>

  <div class="flex">Style : <%= @room.style %></div>

  <div class="flex">Players : <%= @room.current_player_number %> / <%= @room.maxPlayer %></div>

  <div class="flex">Spectators : <%= @room.current_spectator_number %> / <%= Room::MAX_SPECTATORS %></div>

</div>
